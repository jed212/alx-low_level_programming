#include "main.h"

/**
 * print_line - Prints a straight line
 * @n: The number of characters to be printed
 */
void print_line(int n)
{
	int len;

	if (n > o)
	{
		for (len = 0; len < n; len++)
			_putchar('_');
	}
	_putchar('\n');
}
